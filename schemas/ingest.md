# タスク: Ingest (情報の構造化とルーティング)
以下の「未整理の入力データ」を読み込み、以下の処理を行ってください。

1. 情報の核心となるキーアイデアを3行で要約する。
2. この情報が、既存のどのプロジェクトに該当するか推測し、適切なプロジェクトフォルダ内のMarkdownとして保存する（既存の仕様書に矛盾があればAdjustフェーズとして指摘する）。
3. どのプロジェクトにも属さない場合は、新規プロジェクトの `01_Plan.md` の素案として保存する。

【未整理の入力データ】
{{USER_INPUT_OR_NOTEBOOKLM_DUMP}}
